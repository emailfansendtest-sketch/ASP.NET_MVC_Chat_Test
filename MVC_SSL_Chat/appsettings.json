{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "NLog": {
    "throwConfigExceptions": true,
    "autoReload": false,
    "internalLogLevel": "Off",
    "variables": {
      "currentAppName": "",
      "commonLayout": "[${longdate:universalTime=true}Z] [${level:uppercase=true}] [${logger}] ${message} ${exception:format=tostring}"
    },
    "targets": {
      "async": true,
      "logfile": {
        "type": "File",
        "deleteOldFileOnStartup": false,
        "maxArchiveDays": "2",
        "filename": "${specialfolder:folder=CommonApplicationData}/.{var:currentAppName}/logs/Entry-${longdate:cached=true}.log",
        "layout": "${var:commonLayout}"
      },
      "logevent": {
        "type": "EventLog",
        "layout": "${var:commonLayout}",
        "machineName": ".",
        "source": "${var:currentAppName}",
        "eventId": "${event-properties:EventID:whenEmpty=0}",
        "log": "Application"
      }
    },
    "rules": [
      {
        "logger": "*",
        "minLevel": "Trace",
        "writeTo": "logfile"
      },
      {
        "logger": "*",
        "minLevel": "Warn",
        "writeTo": "logevent"
      }
    ]
  },
  "SaveFrequency": "1000",
  "RefreshingFrequency": "15000",
  "VaultReadingAttemptsMax": "5",
  "VaultReadingDelay": "1000",
  "LocalizationFileName": "Localization.resx",
  "AllowedHosts": "*"
}